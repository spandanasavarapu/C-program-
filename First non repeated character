#include <stdio.h>
int main(){
    char s[100];
    int count[256]={0}, i, j;

    scanf("%s", s);

    for(i=0;s[i];i++)
        count[s[i]]++;

    for(i=0;s[i];i++){
        if(count[s[i]]==1){
            printf("%c", s[i]);
            break;
        }
    }
}
