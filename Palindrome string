#include <stdio.h>
int main(){
    char s[100];
    int i=0, len=0, flag=1;

    scanf("%s", s);

    while(s[len]) len++;

    for(i=0;i<len/2;i++){
        if(s[i]!=s[len-i-1]){
            flag=0; break;
        }
    }

    if(flag) printf("Palindrome");
    else printf("Not Palindrome");
}
