#include <stdio.h>
int main(){
    char s[100];
    int i,j,k;

    scanf("%s", s);

    for(i=0;s[i];i++){
        for(j=i+1;s[j];){
            if(s[i]==s[j]){
                for(k=j;s[k];k++)
                    s[k]=s[k+1];
            } else j++;
        }
    }

    printf("%s", s);
}
