#include <stdio.h>

int main(){
    int a[]={1,2,3}, b[]={3,4,5};

    for(int i=0;i<3;i++)
        printf("%d ",a[i]);

    for(int i=0;i<3;i++){
        int found=0;
        for(int j=0;j<3;j++)
            if(b[i]==a[j]) found=1;

        if(!found) printf("%d ",b[i]);
    }
}
