#include <stdio.h>
struct item {
    char name[50];
    int stock;
    float price;
};
int main(){
    struct item i;
    float total;

    scanf("%s %d %f", i.name, &i.stock, &i.price);
    total = i.stock * i.price;

    printf("Total Value=%.2f", total);
}
