#include <stdio.h>
#include <math.h>
int main() {
    int n, i=0, dec=0;

    scanf("%d",&n);

    while(n>0){
        dec += (n%10) * pow(8,i);
        n/=10;
        i++;
    }

    printf("%d", dec);
}
