#include <stdio.h>
int main() {
    int n, i=0, oct[20];

    scanf("%d",&n);

    while(n>0){
        oct[i++] = n % 8;
        n /= 8;
    }

    for(int j=i-1;j>=0;j--)
        printf("%d", oct[j]);
}
